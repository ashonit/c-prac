A set of simple functions to implement in C as practice for someone learning the language. Set up as TDD-style tests.

## Dependencies
On Linux:
gcc

On windows:
This is pretty neat -> github.com/skeeto/w64devkit/releases
